# Enable HTTPS for the React dev server (required for camera/barcode access on mobile).
# The browser only grants camera permission to pages served over HTTPS (or localhost).
HTTPS=true

# API base URL for the CollectorsVault backend.
# Override this in a .env.local file or your CI/production environment.
REACT_APP_API_BASE_URL=https://localhost:5001
